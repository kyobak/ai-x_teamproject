# PROMPTS.md — 에이전트 사용 기록

계획서 협업 규칙: "주요 판단과 프롬프트를 PROMPTS.md 에 기록한다. 설명하지 못하는 코드는 머지하지 않는다."

## 2026-09-18 · 1차 구현 (Claude Code, 모델 Fable 5.1)

**입력 프롬프트 (요약)**
> 계획서(개정 3.1판) 링크를 주며: ① 모바일·웹 프로토타입, CCTV 대신 영상 파일로 시연 ② Figma UI/UX 는 병행이 어려우면 개발 먼저 ③ 코드마다 "왜" 주석 또는 README — 발표 때 코드를 전부 이해해야 함 ④ 사용량 한도 대비 인수인계 파일 ⑤ 프로젝트 폴더 안에서 자유롭게 작업.

**에이전트가 내린 주요 판단**
| 판단 | 이유 | 팀이 검토할 점 |
|---|---|---|
| Supabase 대신 FastAPI + SQLite | 계정·비밀키 없이 노트북 한 대로 시연, 팀원이 SQL 을 직접 읽을 수 있음 | 배포 시 Postgres 로 이전 (ADR-0001) |
| SSE 로 실시간 | 서버→화면 단방향이면 WebSocket 보다 단순 | 양방향 필요 시 교체 |
| 판정 규칙을 서버에 집중 | 펌웨어는 숫자만, 상수 변경 시 재업로드 불필요 | — |
| 세탁 센서는 시뮬레이터로 대체 | 하드웨어 미확보. 프로파일에 불림 구간(3분 무진동)을 넣어 T=5분 규칙을 검증 | 5주차 실측으로 프로파일 교체 |
| 샘플 영상은 Roboflow 공개 데모(보행자) | 공개·저장소 미포함. 줄 구역·통과선을 임의로 그려 파이프라인 동작만 확인 | 연출 영상으로 구역 재설정 |
| Web Push 대신 SSE + Notification API + 화면 배너 | VAPID·HTTPS 없이 로컬 시연 가능, iOS 제약 우회 | 배포 후 Web Push 추가 |
| Figma 는 개발 완료 후 마지막에 | 사용자 지시 ②. Figma MCP(`use_figma`)로 5화면 프레임 + `Tokens` 변수 생성 | 컴포넌트화는 팀이 이어서 (`docs/design.md`) |

**발견한 버그와 수정**
- `/api/queue/me` 가 `/api/queue/{resource_id}` 에 가려져 내 티켓이 빈 목록 → 라우트 순서 변경 + 회귀 테스트.
- 통과선 첫 통과 시 처리율이 분당 70명처럼 튐 → 경과 시간 하한 15초.

**검증**
- pytest 20 passed, `npm run lint`/`build` 통과, 샘플 영상 dry-run(구역 8~10명), 휴대폰 뷰포트 스크린샷, 대기열 호출 배너 확인.

---
(다음 작업자는 아래에 같은 형식으로 추가)
