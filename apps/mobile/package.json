{
  "name": "erica-wait-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "ERICA 대기 통합 — Android 앱 껍데기 (Capacitor). 배포된 웹앱(https://erica-wait.vercel.app)을 앱 안에서 엽니다.",
  "scripts": {
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && ./gradlew assembleDebug && cp app/build/outputs/apk/debug/app-debug.apk ../../../dist/erica-wait.apk && echo 'APK: dist/erica-wait.apk'"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
