# 백엔드 의존성. 루트 .venv 에 설치합니다:  .venv/bin/pip install -r server/requirements.txt
fastapi
uvicorn[standard]
pydantic
httpx      # 테스트에서 TestClient 가 사용
pytest
pywebpush   # Web Push (VAPID) 발송
qrcode[pil] # 오픈스페이스 입구 QR 이미지 생성
requests
